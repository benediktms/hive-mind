import { FormLayout, LoginForm } from '@hive-mind/client-data-access-auth';

export const LoginPage = () => {
  return (
    <FormLayout forPage="login">
      <LoginForm />
    </FormLayout>
  );
};
